# DirectoryStatReadout
Uses AWK (Unix text processing utility) to parse and display a summarized statistics readout for all files in a directory, given through the command 'ls -la --time-style='+%Y-%m-%d %H:%M:%S'
